# ===========================================
# Backend Environment Variables
# ===========================================

# ===========================================
# Database Configuration
# ===========================================
# MongoDB connection string
# For local MongoDB: mongodb://localhost:27017/jobportal
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/jobportal
MONGO_URI=your_mongodb_connection_string

# ===========================================
# JWT Configuration  
# ===========================================
# Secret key for JWT token generation (use a strong, random string)
SECRET_KEY=your_jwt_secret_key_here

# ===========================================
# Cloudinary Configuration
# ===========================================
# Cloudinary cloud name (from your Cloudinary dashboard)
CLOUD_NAME=your_cloudinary_cloud_name

# Cloudinary API key (from your Cloudinary dashboard)
API_KEY=your_cloudinary_api_key

# Cloudinary API secret (from your Cloudinary dashboard)
API_SECRET=your_cloudinary_api_secret

# ===========================================
# Server Configuration
# ===========================================
# Port number for the server (default: 3000)
PORT=3000

# Node environment (development, production, test)
NODE_ENV=development

# ===========================================
# CORS Configuration (Optional)
# ===========================================
# Frontend URL for CORS (only needed if different from default)
# For development: http://localhost:5173
# For production: https://your-frontend-domain.vercel.app
# FRONTEND_URL=http://localhost:5173

# ===========================================
# Additional Configuration (Optional)
# ===========================================
# Session secret (if using sessions)
# SESSION_SECRET=your_session_secret

# Cookie expiration time (in milliseconds)
# COOKIE_EXPIRES=7200000

# ===========================================
# Instructions:
# ===========================================
# 1. Copy this file to .env in the BACKEND directory
# 2. Replace all placeholder values with actual values
# 3. Never commit the .env file to version control
# 4. Keep your credentials secure and private
# ===========================================